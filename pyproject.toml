[project]
name = "wordle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kim", email = "codam" }
]
requires-python = ">=3.10"
dependencies = [
    "prompt-toolkit>=3.0.53",
]

[project.scripts]
wordle = "wordle:main"

[dependency-groups]
dev = [
    "pyright>=1.1.414",
    "ruff>=0.16.7",
]
